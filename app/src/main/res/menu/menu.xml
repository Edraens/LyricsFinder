<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <item
            android:id="@+id/menu_favorites" android:icon="@drawable/ic_bookmarks"
            app:showAsAction="ifRoom" android:title="@string/favorites"/>
    <item
            android:id="@+id/menu_history" app:showAsAction="never" android:title="@string/history">
    </item>

    <item
            android:id="@+id/menu_settings" app:showAsAction="never" android:title="@string/settings">
    </item>
</menu>